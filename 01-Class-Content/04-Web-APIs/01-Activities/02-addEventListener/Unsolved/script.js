// Create a variable to hold the count

// Create variables to select the increment button, decrement button and count heading by their respective ids.

// Create a function that displays the current count on the page

// Create an addEventListener that will increment the count on click
// and calls the function to display the current count

// Create an addEventListener that will decrement the count on click
// and calls the function to display the current count
